# poly2poly

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build poly2poly` to build the library.

## Running unit tests

Run `nx test poly2poly` to execute the unit tests via [Jest](https://jestjs.io).
